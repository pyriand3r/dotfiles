# the vars file needs to be first so that other config files can use the vars
# defined within it.
import vars.conf;

import base.conf;
import startup_apps.conf;
import displayModes.conf;
import workspaces.home.conf
import shortcuts.conf;
#import mousemode.conf;

